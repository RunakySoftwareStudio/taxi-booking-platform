/*=========================================================
  page.tsx keeps the route
  /admin/pricing/[pricingProfileId] working.

  AdminPricingDetailPage.tsx contains the real page code.
=========================================================*/

export const dynamic = "force-dynamic";

export { default } from "./AdminPricingDetailPage";