/*=========================================================
  page.tsx keeps the route /admin/pricing working.
  AdminPricingPage.tsx contains the real page code.
=========================================================*/

export const dynamic = "force-dynamic";

export { default } from "./AdminPricingPage";